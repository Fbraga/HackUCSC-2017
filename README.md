# YouSee: Santa Cruz (HACK UCSC 2017)

Deploying:
Clone the repo, then run `npm install` while in the repo to get all the dependencies.
Start the webserver by running `node index.js` while root (e.g. `sudo node index.js`)
